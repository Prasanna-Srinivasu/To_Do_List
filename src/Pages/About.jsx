const About = () => {
  return (
    <div className="about">
      <h2>About Task Manager</h2>
      <p>
        This Task Manager App allows you to create, manage, and track your tasks efficiently.
        Features include adding tasks, marking them as complete, deleting them, and persisting
        tasks across sessions using localStorage. The app also supports light/dark mode
        and responsive navigation between Home and About pages.
      </p>
    </div>
  );
};

export default About;